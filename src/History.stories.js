import React from 'react'
import History from './History'

export default {
  title: 'History',
  component: History,
}

const Template = args => <History />

export const HistoryEntry = Template.bind({})
HistoryEntry.args = {}
